{
  "name": "web-research-agent-node",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/agent.js --bundle --platform=node --format=esm --outfile=bundle.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
